#!/usr/bin/python2

import time
import sys

from growd.hw.raspberry import Raspberry
from growd.hw.relay import Relay

with Raspberry() as rpi:
    relay = Relay(rpi, 16)

    st = True
    while True:
        time.sleep(2)
        print 'Relay: %s' % st
        relay.switch(st)
        st ^= True
